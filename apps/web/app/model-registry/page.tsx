import { ModelRegistryPanel } from "@/components/model-registry/ModelRegistryPanel";

export default function ModelRegistryPage() {
  return (
    <main className="min-h-screen bg-slate-50 px-6 py-8">
      <ModelRegistryPanel />
    </main>
  );
}